site_name: pylongfellow
watch:
  - src
site_description: Python interface to implementations of Longfellow zero-knowledge proofs over ISO 18013-5 mdoc credentials
repo_url: https://github.com/pipe23-org/pylongfellow

theme:
  name: material
  font: false
  features:
    - navigation.sections
    - content.code.copy

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            docstring_style: google
            show_source: false
            show_root_heading: true
            show_labels: false
            heading_level: 3
            members_order: source
            separate_signature: true
            show_signature_annotations: true

nav:
  - Home: index.md
  - Development: development.md
  - API Reference: reference.md
